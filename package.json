{
  "name": "rss",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start:server": "cd server && npm install && npm start",
    "start:client": "cd client && npm install && wait-on tcp:localhost:3000 && npm start",
    "start": "npm install && npm-run-all -p start:server start:client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "wait-on": "^7.0.1"
  }
}
